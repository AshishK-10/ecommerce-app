<!DOCTYPE html>
<html>
  <head>
    <title>Ecommerce</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body style="width:inherit">
    <main class="flex" style="width:inherit">
      <nav class="flex">
        <div class="w-64 p-4 h-screen bg-gray-600 sticky top-0">
          <div class="flex my-6">
            <%= image_tag 'logo.png', class: 'w-10' %>
            <h1 class="text-md py-2 ml-3 text-white font-bold hidden md:block">Ecomm Admin</h1>
          </div>
          <ul class="flex flex-col text-gray-50">

            <li>

              <% navs_names.each do |x| %>
                <%= link_to x[:path], class: "block px-4 py-2 hover:rounded hover:bg-gray-700" do %>
                  <%= icon('fa-solid', x[:icon]) %> <span class="hidden md:inline-block"> <%= x[:name]%> </span>
                <% end %>
              <% end %>

            </li>

          </ul>
        </div>
      </nav>
      <div class="w-full p-8">
        <%= yield %>
      </div>
    </main>
  </body>
</html>
